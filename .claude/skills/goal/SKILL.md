---
description: Set a session-level goal that all work in the session should serve. Use when the user states an outcome the session must achieve, e.g. /goal the user must have a seamless voice experience.
---

The user's goal for this session: $ARGUMENTS

Treat this goal as the acceptance criterion for all work in the session:

1. Restate the goal in one sentence to confirm understanding.
2. Audit the parts of the codebase that bear on the goal and list the
   concrete gaps between current behavior and the goal.
3. Propose the changes that close those gaps, ordered by impact. If a gap
   requires an architectural decision, a new vendor, or new cost, surface
   it as a question rather than assuming.
4. Implement the agreed changes, verifying each one against the goal
   before calling it done.
5. Before ending the session, summarise how far the goal was advanced and
   what remains.
